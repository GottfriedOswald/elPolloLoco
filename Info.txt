git@github.com:GottfriedOswald/elPolloLoco.git

to do:
-Flaschen mit zufälliger Positionierung
 verteilen

-bestimmte Anzahl von Flaschen soll
 eingesammelt werden können

 -Endboss soll Schaden nehmen
  wenn mit Flasche getroffen

  ________________________________________________________________________
  Funktionen in der movable-object Klasse

  isDying(){
      setInterval(() => {
          this.y -= this.speedY;
          this.speedY -= acceleration_dying;
      },35);
  }